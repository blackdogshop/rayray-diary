# 更新紀錄 — 瑞瑞的成長日記

## 2026-08-27 — 離乳期餵食紀錄優化（大改版）

### 混合餐與熱量
- 每筆餵食可同時填 **奶量 mL / 罐頭 g / 奶粉湯匙 / 額外加水 mL**；表單即時顯示本餐熱量與建議沖泡水。
- 換算：奶粉 12.4 kcal/匙、罐頭 kcal/g（可設定，預設 0.9）；額外加水只計水分不計熱量。
- 每筆記錄顯示 🔥 本餐熱量、加水、😋 食慾（吃光/一半/幾口/不吃）、🌡️ 溫度（冷/常溫/微溫）。

### 首頁
- 中間卡片改「今日攝取」（今日總 kcal + 目標）。
- 新增「今日攝取熱量」卡：目標進度條、原始攝取量、熱量拆解、**離乳比例**（罐頭 vs 奶粉熱量佔比）。
- 摘要改為「餵食 N 次・最後一餐（距現在多久）」。
- 每日熱量目標依最新體重估算（2.5×RER，範圍 2–3×），顯示範圍並標註「估算、以獸醫為準」。

### 圖表 / 體重
- 每日奶量圖＋罐罐圖 → 整合「每日營養攝取」堆疊圖（罐頭/奶粉不同色、柱頂總 kcal、目標虛線）；可切「熱量 / 原始量」。
- 體重圖下方新增 **7 日平均增重**；連續體重未上升會顯示提醒（以獸醫為準）。

### 食物設定（⚙️）
- 右上齒輪開啟「食物設定」：罐頭名稱與 kcal/g、奶粉名稱與 kcal/匙、沖泡 cc/匙，存雲端即時套用。**換品牌只改數字、不用改程式。**

### 資料欄位（新增）
`food(罐頭g)`、`formula_scoop(奶粉匙)`、`water_ml(加水)`、`kcal(本餐熱量)`、`appetite(食慾)`、`food_temp(溫度)`。

> ⚠️ 熱量目標與體重提醒為估算參考，非獸醫建議。皇家離乳罐 kcal/g 預設 0.9，請依罐身標示於「食物設定」調整。

## 2026-08-24

### 新功能
- **離乳罐頭記錄**：新增記錄表單多了「🥫 離乳罐頭 (g)」欄位。
  - 每筆記錄若有吃罐罐，會以綠色顯示克數。
  - 當日日期列會顯示「罐罐 X g」合計。
  - 上方「今日奶量」卡片下方會出現綠色「＋🥫 Xg」。
  - 新增「每日罐罐」綠色長條圖（還沒有任何罐罐記錄時自動隱藏）。
- **💉 預防針 / 其他醫療欄位**：醫療區塊新增此欄，可記錄預防針、驅蟲、下次回診等；存檔後會顯示在該筆記錄上（💉 橘色標籤）。
- **體重圖時間切換**：圖上方可切「最近 7 天 / 14 天 / 全部」，預設 7 天（範圍內不足兩筆時自動退回全部）。
- **體重圖數值提示**：滑鼠移過去（手機點/滑）會浮現該資料點的日期時間與當時體重。

### 體驗優化
- **記錄清單收合**：預設只顯示最近 4 天，可「查看全部 N 天記錄」展開／收合，避免資料變多後一直往下滑。
- **尿尿選項簡化**：已學會貓砂盆，尿尿只保留「有 / 沒有」兩個選項，新增記錄預設為「有」。（過去記錄中「自己尿 / 未確認」仍會照原樣顯示。）
- 文字對比度調高（較符合無障礙標準）、「尿尿」按鈕過窄時自動換行、分段按鈕加上 `aria-pressed`、底部多留空間避免浮動按鈕擋到記錄。

### 資料欄位
每筆記錄目前的欄位：`t`（時間）、`ml`（奶量）、`g`（體重）、`food`（離乳罐頭 g）、`pee`（尿尿：self/yes/no/na）、`poop`、`med`（吃藥）、`topical`（擦藥）、`wound`（傷口觀察）、`vaccine`（預防針/其他醫療）、`note`、`who`（記錄人）。

---

_技術架構：單一 `index.html` 靜態網頁 + Firebase Firestore（即時同步），部署於 GitHub Pages。修改後 `git push` 即自動更新線上版。_
